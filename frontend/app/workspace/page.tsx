import Link from "next/link";
import { AppShell } from "@/components/layout/AppShell";
import { mockBoards, mockUsers, mockWorkspace } from "@/data/mock-data";

export default function WorkspacePage() {
  return <AppShell><div className="page-header page-header--compact"><div><span className="eyebrow">Workspace overview</span><h1>{mockWorkspace.name}</h1><p>{mockWorkspace.description}</p></div><Link className="button button--secondary" href="/dashboard">Back to overview</Link></div><section className="workspace-hero"><div><span className="eyebrow">The room behind the work</span><h2>Four people, one clear direction.</h2><p>Keep boards close, decisions visible, and the next action easy to find.</p></div><div className="workspace-hero__stat"><strong>{mockBoards.length}</strong><span>active boards</span></div></section><section className="workspace-grid"><div className="workspace-panel"><div className="section-heading"><div><span className="eyebrow">Shared spaces</span><h2>Boards</h2></div></div>{mockBoards.map((board) => <Link className="workspace-board" href={`/boards/${board.id}`} key={board.id}><span className="board-card__swatch" style={{ backgroundColor: board.color }} /><span><strong>{board.name}</strong><small>{Object.keys(board.tasks).length} tasks · {board.updatedAt.toLowerCase()}</small></span><span className="arrow">↗</span></Link>)}</div><div className="workspace-panel"><div className="section-heading"><div><span className="eyebrow">The people</span><h2>Members</h2></div><span className="section-heading__count">{mockUsers.length} online</span></div><div className="member-list">{mockUsers.map((user) => <div className="member-item" key={user.id}><span className="avatar" style={{ backgroundColor: user.avatarColor }}>{user.initials}</span><span><strong>{user.name}</strong><small>{user.id === "dev" ? "Owner" : "Contributor"}</small></span><span className="status-dot" /></div>)}</div></div></section></AppShell>;
}
